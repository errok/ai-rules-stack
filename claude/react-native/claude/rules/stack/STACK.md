# Stack rules — React Native

Generic layer shared by every app on this stack (reached through `.claude/rules/stack/`):
everything here must hold for *any* app. Palette values, theme decisions, features and
screens belong to the project (`.claude/rules/project/`). Claude rules link only to Claude
rules (`.md`), never to Cursor files (`.mdc`).

@.claude/rules/stack/core-rules/project-architecture-always.md
@.claude/rules/stack/core-rules/naming-auto.md
@.claude/rules/stack/core-rules/no-unrequested-cleanup-always.md
